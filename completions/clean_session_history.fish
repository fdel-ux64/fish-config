# Completions for clean_session_history

# Options
complete -c clean_session_history -s y -l yes -d "Clear immediately without prompt"
complete -c clean_session_history -s w -l wait -d "Wait time in seconds before clearing" -r
complete -c clean_session_history -s h -l help -d "Show help message"
